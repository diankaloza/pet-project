import styled from 'styled-components'

export const NavBar = styled.nav`
  height: 56px;

  padding-inline: 18px;
  letter-spacing: 3px;
  font-size: 24px;
  display: flex;
  align-items: center;

  color: white;
  background: rgb(60 14 45);

  box-shadow: rgba(50, 50, 93, 0.25) 0px 6px 12px -2px, rgba(0, 0, 0, 0.3) 0px 3px 7px -3px;
`
